apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ui
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      name: http
      nodePort: {{ .Values.ui.nodePort }}
  selector:
    app: {{ .Release.Name }}-ui

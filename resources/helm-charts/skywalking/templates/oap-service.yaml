apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-oap
spec:
  type: ClusterIP
  ports:
    - port: 12800
      targetPort: 12800
      name: rest
    - port: 11800
      targetPort: 11800
      name: grpc
  selector:
    app: {{ .Release.Name }}-oap
